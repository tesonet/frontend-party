import { NAME } from './constants';

export const AUTHENTICATE = `${NAME}.AUTHENTICATE`;
export const AUTHENTICATE_SUCCESS = `${NAME}.AUTHENTICATE_SUCCESS`;
export const AUTHENTICATE_FAILURE = `${NAME}.AUTHENTICATE_FAILURE`;

export const LOGOUT = `${NAME}.LOGOUT`;
