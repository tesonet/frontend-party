export interface ServersListState {}

export const initialState: ServersListState = {};

export const serversList = (
  state = initialState,
  action: any
): ServersListState => {
  return state;
};

