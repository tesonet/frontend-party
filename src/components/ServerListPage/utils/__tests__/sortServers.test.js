import sortServers from '../sortServers';

const unsortedServers = [
  {
    name: 'A server',
    distance: 1000,
  },
];

describe('sortServers', () => {
  it('should sort servers', () => {

  });
});
