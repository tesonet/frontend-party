import { defineMessages } from "react-intl";

const scope = "internalPageHeader";

export default defineMessages({
  logoutButtonText: {
    id: `${scope}logoutButtonText`,
    defaultMessage: "Logout",
  },
});
