// Polyfills for IE11 - could let babel take care of it automatically,
// but I prefer to to know exactly what I'm using
import 'core-js/features/promise';
import 'whatwg-fetch';
import 'formdata-polyfill';
