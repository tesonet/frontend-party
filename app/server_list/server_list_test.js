'use strict';

describe('tesonetApp.server_list module', function() {

  beforeEach(module('tesonetApp.server_list'));

  describe('ServerListCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var serverListCtrl = $controller('ServerListCtrl');
      expect(serverListCtrl).toBeDefined();
    }));

  });
});