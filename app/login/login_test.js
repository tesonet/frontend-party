'use strict';

describe('tesonetApp.authentication module', function() {

  beforeEach(module('tesonetApp.authentication'));

  describe('authentication controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var authCtrl = $controller('AuthenticationController');
      expect(authCtrl).toBeDefined();
    }));

  });
});