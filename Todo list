Todo:
    Critical:
        + basic project structure
        + login inputs
        + app init function
        + 404 page + routing
        + logout knopke
        implement sorting (use lodash sortBy method)
        add debounce to input fields (use lodash debounce)
        input form handle error case (add some text + styled inputs)
        create configuarable store (to test reducer creators)
        add scss
        update user about bad logins
        use createActions
        implement redirect
        implement re-select for list sorting
        implement prettier
    Nice To Have:
        code splitting
        implement route generator
        remove all any
