Todo:
    Critical:
        + basic project structure
        + login inputs
        + app init function
        + routing
        + logout button
        + implement sorting (use lodash sortBy method)
        + add scss support
        + createReducer tests
        + input form handle error case (add some text + styled inputs)
        + tests for axios calls
        + style up page
        + remove idea settings from git
        + install and use icons 
        + add error message to server list
        + add loader to server list
        + add read me file
    Nice To Have:
        + use createActions
        + implement prettier
        + dynamic paths
        - trim bootstrap
        - Add ascending/descending order sorting to server list
        - split vendors from bundle
        - code splitting by route
        - implement route generator
        - implement re-select for list sorting
        - Webpack Bundle Analyzer
        - Move critical html/css to html file
    Consider:
        - Gimp support assets
        - Drop bootstrap (I would love to use styled-components)
